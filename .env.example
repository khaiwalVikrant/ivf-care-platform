# Google AI / Vertex AI credentials
GOOGLE_API_KEY=AIzaSyDoXPD1XN1LeCpAgWurV3F2rbytjpE3AdM

# Google Cloud project for Vertex AI and other GCP services
GOOGLE_CLOUD_PROJECT=ivf-agent

# Vertex AI Search datastore ID for evidence grounding
VERTEX_SEARCH_DATASTORE_ID=ivf-agent-doc-knowledge_1774638429867

# Optional: Google Cloud region (defaults to us-central1)
# GOOGLE_CLOUD_REGION=us-central1

# Optional: Redis URL for Phase 2 session persistence
# REDIS_URL=redis://localhost:6379/0

# Task Manager API — set these in GCP Secret Manager for Cloud Run deployment
# Secret name: task-manager-db-password (just the password, not the full URL)
# ALLOYDB_PASSWORD=your-alloydb-password

# These are set as plain env vars in cloudbuild.yaml (not secrets):
# ALLOYDB_INSTANCE_URI=projects/ivf-agent/locations/us-central1/clusters/ivf-care-cluster/instances/ivf-care-cluster-primary
# ALLOYDB_DB=task_manager
# ALLOYDB_USER=postgres

# For local dev with SQLite (no AlloyDB needed):
# DATABASE_URL=sqlite+aiosqlite:///./task_manager.db

# Secret name: task-manager-secret-key
# SECRET_KEY=your-secret-key-here
